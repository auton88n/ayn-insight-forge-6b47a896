# Full app diagnostic — 2 August 2026

Requested: look into everything, make sure it works, delete dead code. This is that audit. Nothing has been deleted — every item below is reported with its evidence so you can approve batches. World Intelligence's own code and tables were left alone per your instruction, though one finding below (broken cron jobs) touches its data pipeline without touching its code.

Scope actually covered: the job-search product's frontend, the 39 live Supabase edge functions (cross-referenced against the 9 that have source in this repo), pg_cron jobs and their targets, and a spot check (not exhaustive) of the database's 122 tables. Not covered: a full pass on all 122 tables' RLS policies and row usage, Supabase Auth hook / webhook configuration beyond what cron reveals, and the extension beyond what earlier work this session already touched.

Every claim below came from either a live grep against this repo, a live query against the Supabase project (`dfkoxuokfkttjhfjcecx`), or a live HTTP request — not from CLAUDE.md's own claims, several of which turned out to be wrong (its edge function list said 8, there are 39; it said `check-subscription`/`create-checkout`/`customer-portal` were deleted, they're live and working).

## CONFIRMED DEAD — safe to remove, evidence given

**Frontend files, zero references anywhere outside their own definition:**
- `src/hooks/useEmail.ts`, `useIdleDetection.ts`, `usePerformanceMode.ts`, `useFormValidation.ts`, `useSpeechRecognition.ts`
- `src/lib/rateLimiter.ts`, `languageDetection.ts`, `adminApi.ts`, `inputValidation.ts`, `logger.ts`
- `src/components/support/SupportWidget.tsx`, `src/components/landing/ProductIllustrations.tsx`, `src/components/employer/CandidateCardSkeleton.tsx`, `src/components/shared/ComingSoon.tsx`
- `src/constants/tierLimits.ts` (only re-exported by `constants/index.ts`, which nothing else imports either)
- Two dead exports inside a live file: `normalizeDocumentUrl` and `toProxyUrl` in `src/lib/documentUrlUtils.ts`, already labeled "Legacy exports for backward compatibility (deprecated)" in their own comment. Only `isDocumentStorageUrl` from that file is actually used.

**i18n keys, zero `t('key')` call sites in any of the three locale files, confirmed present and identical in all three (en/ar/fr):**
- The entire `tutorial.*` block (25 keys per locale — the deleted eye/onboarding tour)
- `dashboard.chatLoaded`, `dashboard.chatsDeleted`, `error.chatLimitReached` + `error.chatLimitReachedDesc`

**Edge functions, confirmed superseded or already-replaced by name in their own or another function's source comments:**
- `verify-admin-pin`, `set-admin-pin`, `admin-pin-alert` — `admin-auth-pin`'s own header says it replaces all three. Their only client-side counterpart, `src/lib/adminApi.ts`'s generic invoke wrapper, is itself dead (listed above), so this is one clean supersession event: delete the three functions and `adminApi.ts` together.
- `delete-account` — `src/components/settings/PrivacySettings.tsx`'s own comment documents the switch to the `self_delete_account` RPC. Zero call sites anywhere.
- `track-visit` — `src/App.tsx` has its own comment: "v3.34.0 — visitor tracking removed. It posted to an edge function that was deleted in v3.21.0." The function is still deployed and ACTIVE; the frontend just stopped calling it. Delete the function to match what the frontend already did.

## PROBABLY DEAD — strong signal, take one more look before removing

- `ayn-market-intelligence` and `resource-monitor` edge functions: **do not exist** (confirmed via live `curl -X OPTIONS`, both return HTTP 404), yet `pg_cron` calls `ayn-market-intelligence` on two schedules (every 2 hours, and weekly) and `resource-monitor` hourly, and `keep-warm` also pings both every 5 minutes. Every one of these calls has been failing — checked `net._http_response`, the last several hours are unbroken 404s. This isn't "probably dead," it's confirmed dead as deployed infrastructure; it's in this tier rather than the one above only because deleting the *cron jobs* (as opposed to code) is a schedule change to production automation, which felt like it deserved your explicit sign-off rather than being bundled with pure code deletions.

## NEEDS YOUR CALL — ties to a product/infrastructure decision, not a cleanup call I can make alone

**The `ayn-*` intelligence pipeline has real drift between what's scheduled and what's deployed.** Nine `pg_cron` jobs call edge functions that return 404: `ayn-super-brain` (9 separate cron entries, different `mode` payloads — news/gov/sectors/startups/jobs/supply/realestate/consumer/geo/health/tech), `ayn-pulse-engine`, `ayn-world-signals`, `ayn-osint-feeds` (2 schedules), `ayn-global-intelligence`, `ayn-proactive-loop`, `ayn-follow-up-agent`, plus the two already listed above. All confirmed via live curl (404) and `net._http_response` logs (repeated recent failures). `keep-warm`'s own source has a comment — "v6: removed dead ayn-prediction-engine" — showing this has happened before: a function gets retired and the things that call it don't get cleaned up in the same pass.

I did not touch any of this, per your instruction to leave World Intelligence alone, but it means several of the ~20 World Intelligence tables (`ayn_market_snapshot`, `ayn_world_signals`, etc.) are likely going stale silently — their refresh jobs are failing every time they fire. Whether the right fix is "redeploy the missing functions" or "these were intentionally retired, clean up the 9 dead cron jobs" is a call only you can make, since I can't tell which state was intended from here.

**`ayn-unified`** — correcting something I told you earlier this session: I'd flagged this as an unexplained function with no matching source anywhere in this repo, created same-day, and said it needed your review before anyone assumed what it was. Since then I found `keep-warm`'s own source: it deliberately keeps `ayn-unified` warm every 5 minutes with `{ intent: 'chat', _internal_warm: true }`. That means it's intentional, currently-maintained infrastructure, not a mystery — but it's still undocumented (no source in this repo, not in CLAUDE.md, and I have not opened whatever the Lovable-side project holds for it). Worth you confirming what it does and whether this repo should carry its source, but it's not the security concern I originally flagged it as.

**Already flagged as separate follow-up tasks earlier this session** (chips should still be visible; not re-litigated here): Resume Hub's own Jobs tab bypasses the credit system entirely (free tailoring via an older code path than the extension uses), and the "Usage & Limits" card in Settings shows a permanently-stuck fake quota from `user_ai_limits`/`user_subscriptions`, two tables with no real writers, sitting right next to the real credit numbers added this session.

**Full 122-table sweep not done.** I spot-checked ~25 tables based on what the frontend/edge-function sweeps surfaced as suspicious. A complete pass (every table's actual read/write call sites, RLS policy sanity, zero-row tables that might be entirely unused features) would be a substantial separate piece of work — tell me if you want that as a next phase.

## CONFIRMED LIVE — flagged as suspicious, checked, not dead

- `src/contexts/SubscriptionContext.tsx` — still wraps the whole app in `App.tsx`, consumed by `AccountPreferences.tsx` and `SubscriptionSuccess.tsx`. (This session's earlier pass nearly "fixed" this based on a CORS artifact specific to local dev — it's fine in production. Reverted before it shipped.)
- `check-subscription`, `create-checkout`, `customer-portal` edge functions — genuinely deployed, genuinely respond, gated by CORS to `https://aynn.io` only (which is exactly why local dev testing made them look dead).
- All of `src/components/dashboard/` — not orphaned; `ProfileAvatarUpload.tsx` and `UsageCard.tsx` feed `AccountPreferences.tsx`, and the `simulator/` subfolder (10 files) feeds `WorldIntelligence.tsx`.
- All of `src/components/admin/` — fully wired into `AdminApp.tsx` → `/manage-bae76e99d97e188b`.
- `app_settings` table — admin-auth-pin's PIN lockout state, not the ten dead `system_config` keys removed in v3.27.0.
- `custom_orders` table — `sign-document`'s legacy NDA/signing flow (`/nda/:token`), still a live route.
- `faq_items` table — Support page.
- `send-email`, `check-email-exists`, `file-upload`, `send-contact-email`, `support-bot`, `send-ticket-notification` edge functions — all called from live, reachable UI (AuthModal, Contact, Support, TicketForm, useFileUpload).
- `admin-notifications` edge function — real cron target (daily 8am digest), not dead despite zero frontend call sites.

## Proposed next step

Tell me which tier(s) to act on. My suggestion: take the CONFIRMED DEAD tier now (it's genuinely zero-risk — every item has a direct evidence trail and nothing references it), hold the cron/pg_cron deletions and the `ayn-*` pipeline until you've decided whether to redeploy or retire it, and decide separately whether you want the full 122-table sweep as another pass.
