# v3.55.0 About page opening rewritten, "Mission and vision" added

Exact copy supplied directly. `About.tsx`'s opening paragraph (generic "we believe applying should be about whether you can do it") replaced with a concrete founding story: AI made applying effortless, a hiring manager who used to read forty applications started opening six hundred and reading none of them properly, the person who could do the job got buried with nobody able to find them, so AYN got built. A `SectionHeading`-labeled "Mission and vision" block follows directly: "Replace volume with evidence. Build a hiring market where being seen depends on what you have done, not on how many places you applied." Deliberately the one labeled section on this page, an exception to v3.54.0's "no pitch labels" call since it's a genuine single mission statement rather than a three-part narrative. Everything else (hero card, the "what it does" and discoverability paragraphs, the CTA) unchanged.

# v3.54.0 About page rewritten from pitch structure to plain mission statement

Reported directly: the three `SectionHeading` labels on `/about` ("The problem", "How we got here", "What it does", from v3.50.0/v3.52.0) read as a pitch-deck narrative, not an actual about-the-company page. Asked what content should replace it; kept the mission/what-AYN-believes framing, dropped the three-act structure. `About.tsx`'s labeled sections are now three unlabeled flowing paragraphs (same facts: why AYN exists, what it does for seekers and employers, the discoverability privacy note) directly under the existing hero card. Hero card and CTA unchanged; `SectionHeading` itself untouched, still used on `/help` and `/contact`.

# v3.53.0 Contact page is a real form, plus a guest RLS bug found underneath it

Asked directly to drop the postal address from `/contact`, replace the plain "email us" block with an actual form, and point every other page that told someone to contact AYN at that same form. `Contact.tsx`'s "Postal" section is deleted; "Email" is replaced with the existing `TicketForm` component (previously only ever used signed-in, inside `Help.tsx`). `Help.tsx`'s raw `mailto:` links now point to `/contact`, and its own signed-in-only embedded copy of the form was deleted as redundant (with the now-unused `signedIn` state). `TermsModal.tsx` still has a bare `support@aynn.io` but is confirmed dead code, left alone. Legal document markdown and the registered address inside it are a separate legal-notice channel, not touched.

Simulating a real guest submission directly against Postgres (not just reading the client code) surfaced three compounding RLS bugs that meant `TicketForm`'s guest branch had never actually worked, in any release: (1) `has_duty_access(uuid)` was referenced by a `{public}`-role SELECT policy on `support_tickets`/`ticket_messages` but never granted `EXECUTE` to `anon`, so `insert().select()` as a guest threw a hard permission error; (2) `INSERT ... RETURNING` re-checks the new row against SELECT policies, and no policy gives a guest visibility into a ticket it just created, so `.select()` failed RLS even once the insert itself passed; (3) `ticket_messages`' insert policy re-queried `support_tickets` in a subquery that is itself subject to `support_tickets`' RLS, hitting the same invisible-row problem for a guest's own opening message. Fixed in `supabase/migrations/20260804120000_allow_guest_ticket_messages.sql`: the `has_duty_access` grant, and a new `SECURITY DEFINER` helper `ticket_belongs_to_caller()` (same pattern as `has_duty_access`/`has_role`) replacing the subquery. `TicketForm.tsx` itself now generates the ticket id with `crypto.randomUUID()` and drops `.select()` on both inserts entirely, so nothing needs reading back under RLS at all; the email field is now `required`, matching the guest policy's own `guest_email` requirement; copy changed from internal "ticket" language to plain "Send message" / "Message sent".

Verified live as a real signed-out visitor: submitted `/contact`'s form, confirmed both rows landed by direct query, then confirmed as a negative test that an anonymous caller still cannot post into a real authenticated user's ticket. Test rows deleted after.

# v3.52.0 About page placeholder heading + section branding

Reported live from a screenshot of `/about`: the first body section was headed "The hook", a leftover copywriting-process label from the v3.50.0 draft, never replaced with real copy. Renamed to "The problem" (what the section's own text is actually about). Same report flagged the marketing pages as unbranded past the hero: `About.tsx`, `Help.tsx` and `Contact.tsx` all used a plain black `<h2>` for every body section with no ember accent anywhere below the top of the page. New `src/components/shared/SectionHeading.tsx`: a small ember bar beside the heading text, dropped in for all 9 section headings across the three pages (previously each was a bare `<h2 className="text-2xl font-semibold tracking-tight ...">`). Verified live on `/about`, `/help`, `/contact`.

# v3.51.0 footer Company column

Both footers' "Company" column had drifted into `NAV_LINKS`: Home, How it works, Pricing, About, Help Center, Contact, a mix of primary nav and company pages with no Legal entry, while Legal sat in its own separate column (Privacy Policy, `/legal` hub, Cookie choices). Asked directly to group About Us, Help Center, Legal and Contact Us under a real "Company" heading. `src/components/shared/siteLinks.ts`'s `NAV_LINKS` is now just the three primary-nav items (Home, How it works, Pricing); a new `COMPANY_LINKS` array holds About Us, Help Center, Legal, Contact Us. Both `LandingSections.tsx`'s `.lp-footer` and the shared `Footer.tsx` render "Navigate" (`NAV_LINKS`) and "Company" (`COMPANY_LINKS`) as two columns; Privacy Policy and Cookie choices moved into the bottom bar beside the copyright line rather than keeping a now-mostly-empty Legal column. New `.lp-footer-bottom-links` CSS rule for the landing footer's bottom row. `Header.tsx`, the site's actual top nav bar, was left untouched — this was scoped to the footer only. Verified live on `/` and `/pricing`.

# v3.50.0 marketing pages, About, Help Center, Contact

Three public pages, written to the founder's exact copy, matching the landing page look (paper white, ember accent, Outfit style headings).

/about, src/pages/About.tsx. A short company overview block set apart from the body ("Hiring runs on volume. We think it should run on evidence.", built by a team in Canada), then The hook, How we got here, What it does, and a Start free call to action. Title tag About AYN.

/help, src/pages/Help.tsx. Replaces the deleted src/pages/Support.tsx, which claimed "Chat with our AI assistant"; that chat was removed in v3.8.0 and the claim is now gone with it, along with the retired trilingual translations block. Five sections (Getting started, Credits and billing, Being found by employers, Your data, For employers) plus Still stuck, filtered client side by a single search box, no backend, with FAQPage plus BreadcrumbList JSON-LD generated from the same array the page renders so the two cannot drift. The existing ticket system stays available: TicketForm renders under "Send us a message" for a signed in visitor only. Response times are stated as aims, matching /sla.

/contact, src/pages/Contact.tsx rewritten from scratch. The old page's project agency copy ("Tell us about your project and we'll help transform your vision into reality") and its French and Arabic strings are deleted, together with the contact form and its contact_messages insert. The page is now one inbox, support@aynn.io, with Before you write, Specific requests (privacy, copyright, security, employer access), Postal, and What we do not have.

Wiring. /support is a redirect to /help in src/App.tsx, and both /help and /about are added to server.js's known route list and to public/sitemap.xml, with the old /support sitemap entry removed. About, Help Center and Contact are in NAV_LINKS in src/components/shared/siteLinks.ts so the footer cannot drift. The two in app links that pointed at /support (NotFound.tsx and DoNotSell.tsx) now point at /help.

Verified live against the dev server with Playwright: /support redirects to /help (final URL /help, h1 "Help Center"), /about, /help and /contact all render with the right title tags (About AYN, Help Center, Contact AYN), the Help search filters sections as you type, and there were no console errors on any of the four loads.

# AYN AI platform map (everything besides the extension and Resume Hub)

## Public copy (v3.29.1 corrections)
Public text has to match what shipped. The landing employer pricing block is still the single public source, and it now names the real allowances from the plans table instead of claiming unlimited searching: free month 25 searches and 5 proposals, Starter 100 and 10, Growth 400 and 40, Scale 1200 and 120. AuthModal describes the employer path as a short intake about the role and no longer says "AYN chat", which has not existed since v3.9.0. Handoff.tsx and the extension handoff toast describe reading, scoring and tailoring, never autofill, which has not existed since v3.0.0. SubscriptionSuccess sends people to "/" and the button says Continue to AYN, because /dashboard redirects. Both role pickers now read "I am looking for a job" and "I am hiring", and the ellipsis character is the single style.

### Company name and footer links (v3.34.0)
Three different names were live at once: "AYN Intelligence" in the landing footer, "Advanced AI Solutions" in the shared footer, "AYN AI" (the registered name, the one the legal documents use) nowhere else. src/components/shared/siteLinks.ts is now the one place COMPANY_NAME ("AYN AI"), COMPANY_TAGLINE, COPYRIGHT_LINE and LEGAL_LINKS live. Both footers read it: src/components/landing/LandingSections.tsx (the inline `<footer>` at the bottom of the landing page, entirely separate from the component below) and src/components/shared/Footer.tsx (used by /pricing, /contact, /do-not-sell, /resume-match and every LegalPage). LEGAL_LINKS now includes Security and Subprocessors and Copyright, which were missing from the footer before this release. footer.copyright in src/i18n/en.ts, ar.ts and fr.ts still holds the old "AYN Intelligence" copy; nothing reads that key any more now that both footers render COPYRIGHT_LINE directly, so it is dead rather than wrong, and has not been deleted.

### Landing footer rebuilt (v3.48.0)
The landing page's own inline `<footer>` (`src/components/landing/LandingSections.tsx`, separate from `src/components/shared/Footer.tsx` per the v3.34.0 note above) was still just `LEGAL_LINKS` (7 items) plus a Cookie choices button plus hardcoded Contact/Pricing links wrapped in one flat row next to the copyright line — no logo, no columns, easy to mistake for broken CSS. Rebuilt to match `Footer.tsx`'s actual structure (brand block with logo/tagline/social icons, then grouped link columns) using this page's own `.lp-*` CSS system rather than Tailwind. New rules in `src/index.css`: `.lp-footer-top`, `.lp-footer-brand`, `.lp-footer-social`, `.lp-footer-cols`, `.lp-footer-col`, `.lp-footer-bottom`, replacing the old two-rule `.lp-footer-row`/`.lp-footer a`. Now reuses `NAV_LINKS` from `siteLinks.ts` for the "Company" column instead of hardcoding Contact/Pricing separately, and column headings use the ember accent already established by `.lp-eyebrow` elsewhere on the page. Verified live at desktop and 375px mobile: clean stack, ember hover states, no console errors.

### Legal footer column simplified (v3.49.0)
Both footers' Legal column still listed all seven `LEGAL_LINKS` documents plus Cookie choices right after v3.48.0 shipped — narrower than before, still a wall of links. `src/pages/LegalIndex.tsx` (route `/legal`, reads `LEGAL_DOCS` from `src/lib/legalDocs.ts`) already existed as a working hub page listing every document with title, description, version and effective date, but nothing linked to it from either footer. `LandingSections.tsx`'s `.lp-footer-col` and `Footer.tsx`'s Legal `<ul>` both now render exactly three items instead of mapping `LEGAL_LINKS`: a direct Privacy Policy link, a single Legal link to `/legal`, and Cookie choices (the consent preferences dialog, not a document, kept separate). Neither file imports `LEGAL_LINKS` any more. `LEGAL_LINKS` and `LegalIndex.tsx` themselves are unchanged; the hub still needs the full list. Verified live on `/` and `/pricing`: three-item column, Legal link lands on the hub page, `tsc --noEmit` clean.

## The chat (v3.8.0: it is candidate search, nothing else)
There is exactly ONE conversational surface in the product's web app, and it belongs to employers. It takes an employer from "I have a role to fill" to "here are the three best candidates". It is not a career coach, not a general assistant, and it does not chat about anything else.

SEEKERS HAVE NO DASHBOARD CHAT. v3.8.0 removed it. Index.tsx AuthedShell now sends a signed in job seeker straight to /resume-hub. The seeker talks to AYN through Ask AYN in the extension, grounded in one real job description plus their resume, which is unchanged. Deleted with it: the ayn-unified edge function (index.ts, jobContext.ts, systemPrompts.ts, llmGateway.ts, emotionDetector.ts, memoryHandler.ts) and src/hooks/useCopilotStarters.ts. The eye UI files (src/components/eye/, src/components/dashboard/, src/hooks/useMessages.ts, useAYN.ts, src/lib/aynPersonality.ts) are still in the repo but have no route reaching them.

WHY. A real session produced three paragraphs of career direction with headings, unsupported flattery ("a perfect fit", "a huge asset"), and an offer to "find specific open roles in Toronto", a capability that does not exist anywhere in the product. A general purpose chat promises things the product cannot do, so the general purpose chat is gone.

WIDGET INTAKE (src/components/employer/IntakeWizard.tsx). The employer does not free type a role. They answer one question at a time by clicking, with a "type it instead" escape on every step. An opening description is optional and, when given, is run through employer_spec_extract so answered questions are skipped. Sequence:
1. Role title. Text with common title suggestions.
2. Seniority. Buttons: intern, entry, mid, senior, staff or principal, manager, director or above.
3. Must have skills. Chips, max 6, autocompleted from employer_skill_catalog, which only returns skills that exist in candidate_skills for opted in candidates, with a live count per skill so over-constraining is visible.
4. Nice to have skills. Same widget, max 6, optional.
5. Location and remote. Buttons on site / hybrid / remote, plus a location field unless fully remote.
6. Employment type. Buttons: full time, contract, part time, internship.
7. Minimum years. Buttons: any, 2 plus, 5 plus, 8 plus, 10 plus.
8. Work eligibility. Country plus must already be authorised / open to sponsoring.
Then an editable JobSpec summary card. Clicking any line reopens that single widget. "Find candidates" calls employer_match.

RESULTS AND EVALUATION (v3.9.0, src/components/employer/CandidateAskCards.tsx, employer_card_answer). There is no free-form chat anywhere in the product any more. The results chat was removed because it described the role wrongly, contradicted itself on years of experience, and leaked internal refs like c1. Under each candidate sits a row of four question cards: Why this score, What is missing, Compare to the others (hidden when only one candidate came back), What to ask in a screen. Each is one stateless server call grounded in the stored search row: the role line is hard-coded into the prompt from the JobSpec, years of experience is asserted as fact when recorded, a missing detail is reported as not available and never guessed. Answers are cached client side per candidate and card. The model never sees names, emails, phones or user ids.

THE PROPOSAL MESSAGE (v3.9.0). Opening the proposal dialog fires employer_draft_proposal, which returns a written message citing only the matched requirements. The employer edits it. If drafting fails, the box stays empty and sending still works.

OFF TOPIC RULE. Anything that is not defining the role or evaluating the returned candidates gets one short sentence saying AYN only helps find candidates for a role, then the current intake question again. No answer to the off topic question, no lecture.

CAPABILITY DENIALS, stated plainly when asked. AYN cannot search job boards. It cannot find open roles. It cannot contact candidates beyond sending a proposal. It cannot see candidates who have not opted into discovery.

EVIDENCE RULE. No praise without evidence. Every statement about fit points at something in that candidate's returned profile. No "perfect fit", no "huge asset", no "exactly what you are looking for". Voice rules unchanged: no markdown symbols, no asterisks, no bullet characters, short sentences, no em dashes, no en dashes.

## World Intelligence and agent society
/world-intelligence (src/pages/WorldIntelligence.tsx + components/dashboard/simulator/): MiroFish-style swarm simulator, five stages Seed -> Graph -> Simulate -> Report -> Chat, driven by useEnginSim against engin.aynn.io (external engine, src/lib/enginApi.ts). The ayn-agent-society edge function is the world simulation backend: modes simulate (layers 1 to 6, batched), get_agents (world_personas table, 73 seeded personas), chat (in character), get_conversations, get_messages.

## Subscriptions and credits
src/contexts/SubscriptionContext is the retired consulting product's billing system: SUBSCRIPTION_TIERS (Starter, Pro, Business, price ids like price_1TBzZNDBJlSjDe8AwUvuHkfn) has nothing to do with the current job search plans in public.plans, and the "5 messages/day" language it shows in Settings > Account (via AccountPreferences.tsx's Usage & Limits card, reading useUsageTracking, which itself reads user_ai_limits and user_subscriptions, two more tables nothing in the current product writes) and on /subscription-success predates AYN's current pricing entirely. Its edge functions (check-subscription, create-checkout, customer-portal) are still genuinely deployed and respond, gated by CORS to https://aynn.io only, so they are not dead in the sense of returning 404, they are just checking a Stripe product catalog that is not this product's. Left as is rather than fixed in v3.35.0, because AccountPreferences.tsx and SubscriptionSuccess.tsx are its only two consumers and neither one calls startCheckout, startTopUp or openCustomerPortal, so nothing user facing is broken by it being wrong, only confusing. The real subscriptions and credits system is Stripe through stripe-billing and stripe-webhook, described below and in Billing self service.

### Credit functions locked against concurrent overspend (v3.42.0)
`billing_ensure`, `credit_spend` and `credit_grant` (`supabase/migrations/20260731122450_*.sql`, patched in `20260803180000_lock_credit_functions.sql`) each did a plain read-balance-then-write with no row locking: read `credit_balance` (a live `SUM(delta)` over `credit_ledger`), decide, then insert a ledger row. All three are independently callable — `resume-hub` calls all three directly, `stripe-webhook` calls `credit_grant` directly, `credit_spend` also calls `billing_ensure` internally — so two calls for the same user close enough in time (double click, two tabs, a retry after a slow-but-successful response) could both read the same starting balance before either committed, both pass the sufficiency check, and both deduct: a real overspend, surfaced while investigating an odd `balance_after` in the v3.41.0 admin walkthrough and flagged there rather than fixed on the spot.

Fixed with `PERFORM pg_advisory_xact_lock(hashtext(_user_id::text));` as the first statement in all three functions. Advisory transaction locks need no schema change, serialize only calls for the same user (different users never contend), auto-release at the end of the transaction even on error or exception, and work for a brand-new user with no `subscriptions` row yet — locking an existing row was not an option there, since `billing_ensure` is what creates that row in the first place. The lock is reentrant per transaction, so `credit_spend` taking it and then calling `billing_ensure` (which takes the identical lock key again) is a safe, instant re-acquire, not a deadlock.

Verified with two synthetic `credit_spend(10)` calls against a shared starting balance of 10 for a throwaway, non-auth-backed uuid (`credit_ledger.user_id` has no FK, so no real account was needed): the first call's own `billing_ensure` call correctly auto-granted the plan's one-time `period_grant` (this test id had never been through `billing_ensure` before, matching the same first-spend quirk noted in the v3.41.0 entry), bringing the true balance to 16 before spending 10 down to 6; the second call then correctly read the post-lock balance of 6, saw it was short of the 10 requested, and was refused rather than allowed through. Final ledger sum matched the returned balance exactly in both calls — no lost update, no negative balance. Test rows deleted afterward.

### Payment receipt email (v3.44.0)
`stripe-webhook`'s `invoice.paid` handler previously called `credit_grant` and nothing else — a paid renewal or a new subscription produced zero email of any kind. It now also calls `sendReceiptEmail` (defined in the same file) right after the grant succeeds: plan name, amount charged (formatted from `invoice.amount_paid`/`invoice.currency`), and credits added, using the same `_shared/emailTemplate.ts` shell the v3.43.0 auth emails use. Best effort, wrapped in try/catch separately from the grant itself, so a Resend failure can never undo a charge or a grant that already committed. Not live-tested: doing so needs either the `STRIPE_WEBHOOK_SECRET` (not available here, only the deployed function holds it) to forge a validly-signed webhook call, or an actual Stripe test purchase, which was out of scope to attempt. Rests on code review and the same `sendBrandedEmail` plumbing already proven live by the proposal/accept notification tests below.

### Metering visibility and real sessions (v3.35.0)
Two things resume-hub already computed but nothing showed, and one thing that only looked shown.

CREDIT VISIBILITY. ext_bootstrap now returns credits: { balance, costs: { tailored_resume, cover_letter } } (previously only smart_tailor and ext_cover_letter_text returned an updated balance, and only after a spend), so extension/sidepanel.js can show it on load, not only after a generation or a failure. The pill lives in the header next to the sign out button, turns from ember to red under COST_TAILOR, links to aynn.io/billing, and updates itself from the credits field already present on a successful smart_tailor or ext_cover_letter_text response. Verified live: signed a throwaway account in through the real extension token flow, ext_bootstrap returned balance 6 for a fresh Free account. Two same-shaped pills were added on the web side reusing calls that already existed: src/pages/ResumeHub.tsx calls billingApi.seeker() once on mount and shows a "N credits" pill next to Your resume; src/pages/EmployerHub.tsx calls billingApi.employer(org.id) after the org loads and after every search, proposal and assessment send, and shows searches / proposals / assessments used against the plan limit in the header, linking to Billing. Confirmed while wiring this up: Resume Hub's own Jobs tab (the "tailor" and "cover_letter" hub-lane actions JobsTab.tsx calls) is a separate, older implementation from handleSmartTailor / handleCoverLetter and never calls creditSpend, so generating from Resume Hub itself is free and unmetered while the identical output from the extension costs credits; flagged as a separate follow up, not fixed here, because it is a metering/product decision, not a display gap.

REAL SESSIONS. src/components/settings/SessionManagement.tsx and its data hook, src/hooks/useUserSettings.ts, read device_fingerprints, a table nothing in this codebase has ever written a row to (checked: zero writers, only this one reader). Active Sessions was therefore permanently empty, and Sign Out All deleted zero rows from that table and then called supabase.auth.signOut() with no scope, which only ever signed out the current tab, not any other device, despite the confirm dialog already promising "including this one." Two new SECURITY DEFINER RPCs read and write the real thing: self_list_sessions() returns every auth.sessions row for auth.uid() (id, created_at, refreshed_at, not_after, user_agent, ip, is_current from comparing to the session_id claim in the caller's own JWT), and self_revoke_session(p_session_id) deletes one session's row and revokes its refresh tokens, scoped to the caller's own sessions only. "Sign Out All" now calls supabase.auth.signOut({ scope: 'global' }), GoTrue's own every-session revoke, rather than reimplementing it. Verified live with two real concurrent sessions on a throwaway account: self_list_sessions showed both with the correct is_current flag; self_revoke_session on the non-current one made its refresh token come back refresh_token_not_found on the next refresh attempt; global sign out, called from one session's token, also invalidated a separate third session's refresh token. Also verified through the actual UI, not just curl: the Sessions tab in Settings showed "Chrome on macOS, This device, last active, IP", and clicking Sign Out All produced a real GoTrue 204 and redirected to the landing page signed out.

### The 45 second cold load, investigated (v3.35.0)
Could not reproduce 45 seconds. Measured directly against https://aynn.io with the Navigation Timing API on a signed out landing visit: domContentLoaded ~1.14s, load ~1.16s, 29 JS chunks, ~540KB total transfer. Two real but minor things were found and one was fixed: useFeatureFlags' mount effect called get_feature_flags unconditionally, so a page with two components independently calling useFeature() paid for two full round trips instead of one even though the module level cache already had a fresh, loaded answer; it now only fetches when the shared cache has never loaded. src/App.tsx's PreloadRoutes warms WorldIntelligence, Settings and Pricing chunks via requestIdleCallback, which is a deliberate, idle-scheduled prefetch, not obviously wrong, and was left alone.

The structural finding that actually explains a report like this: the project's database is eu-west-2 (confirmed via get_project), but every edge function response carries x-sb-edge-region: us-east-1, checked three separate times against both resume-hub and stripe-billing. Every DB query inside every edge function call pays a transatlantic round trip on top of whatever the query itself costs, and resume-hub's handlers issue many queries per call, several of them sequential rather than batched. A signed in dashboard load fires multiple separate edge function invocations on mount (ext_bootstrap-equivalent calls, billing, proposals, assessments), each paying its own cross region tax plus a possible cold Deno isolate start after a quiet period; that combination, not anything on the public landing page, is the plausible source of a report this severe, and was not reproduced here because reproducing a cold isolate on a project with recent traffic is not something this investigation could force. Whether Supabase's function region can be pinned to eu-west-2 for this project is a platform setting, not a code change, and was not touched.

### Billing self service (v3.34.0)
supabase/functions/stripe-billing actions, all keyed off the one Stripe customer per email: checkout (subscription Checkout session for a plan_key), portal (the hosted Stripe customer portal for the card on file), cancel (v3.30.0, sets cancel_at_period_end), resume (undoes cancel_at_period_end before the period ends, existed since v3.30.0 but nothing called it until now), change_plan (moves the active subscription's price up or down, or down to Free by setting cancel_at_period_end when the target plan has no price or is 0 cents; moving down to Free with no active subscription is a no-op that just reports the move), state (the live subscription: status, cancel_at_period_end, current_period_end, price_id, or null if there is none) and invoices (up to 24 from Stripe, each with amount_paid, status, hosted_invoice_url and invoice_pdf). Plans and their Stripe price ids stay entirely in public.plans; nothing is hardcoded in the function.

src/lib/billing.ts wraps all seven as billingApi.checkout / portal / cancel / resume / changePlan / state / invoices, plus the existing plans / seeker / employer / upgradeIntent calls into resume-hub. src/pages/Billing.tsx renders the full tier grid for the signed in audience, including the employer tiers Growth and Scale that /pricing does not publish; a Keep my subscription button appears in place of Cancel whenever cancel_at_period_end is true; a Billing history card lists invoices with a receipt link; and every plan-change confirm dialog states the founder decision in words: moving up applies now with Stripe prorating the difference, moving down or to Free keeps the current plan running until the period already paid for ends and then stops, no proration, no refund, and unused credits expire at the end of the period and do not roll over.

## Legal and consent machinery (v3.30.0)
Four pieces, all new, all promised by the Terms and the Privacy Policy.

COOKIE CONSENT. Google Analytics no longer loads in the static head of index.html. src/lib/analytics.ts owns the tag: readCookieConsent, writeCookieConsent, loadAnalytics and initAnalyticsFromConsent, called once from src/main.tsx. Nothing is fetched from googletagmanager until an explicit accept. src/components/shared/CookieConsent.tsx renders the banner, mounted once inside BrowserRouter in src/App.tsx. Accept and Reject are the same size, the same styling and one click each, nothing is pre-ticked, and strictly necessary cookies for auth, session and security are named in the copy as exempt. The choice is stored in localStorage under ayn-cookie-consent as { choice, version, decidedAt, gpc } with COOKIE_CONSENT_VERSION 1.0, so a version bump reprompts. navigator.globalPrivacyControl true is recorded as a reject and the banner never appears. openCookiePreferences() dispatches ayn:open-cookie-preferences, wired to the "Cookie choices" link in both footers.

DO NOT SELL OR SHARE. New public route /do-not-sell, src/pages/DoNotSell.tsx, linked from the shared Footer and the landing footer. It states plainly that AYN does not sell personal information and does not share it for cross context behavioural advertising, and tells a California resident how to exercise their CCPA rights by email or through support. It is not a form that does nothing.

CONSENT RECORD (v3.33.0, supersedes the v3.30.0 arrangement). Why it broke first: the only writer to terms_consent_log was useAuth.acceptTerms, called from the TermsModal inside the legacy dashboard shell, and when seekers were routed to /resume-hub and that shell was deleted the writer became unreachable, which is why the table stopped at 8 rows on 7 May 2026; the v3.30.0 replacement, a browser call to legal_consent_record after signUp, never fired either, because with email confirmation on there is no session at that moment and getSession() returns null. Both dead paths are gone. The version truth is the markdown: src/lib/legal.ts reads version and effective date out of src/content/legal/terms.md and privacy.md through parseDocMeta, so amending a document is the only way to change what gets recorded (Terms 1.0, Privacy 1.0, effective 1 August 2026). The acceptance itself is written server side by handle_new_user, in the same transaction as the auth.users row, from the signup metadata terms_version, privacy_version, legal_accepted and consent_user_agent that AuthModal attaches; if that insert fails the signup fails. A signup that presents no acceptance, meaning OAuth or an account created by hand, still gets a row, with null versions, both flags false and source 'unrecorded' or 'oauth_unrecorded', so no account exists without a record and no acceptance is ever invented. The IP is the one thing the browser cannot know, so attachConsentIp posts legal_consent_record on the first authenticated call and that action now completes the existing row rather than inserting a second one, inserting only for a genuine re-acceptance. terms_version is nullable now, which the honest rows need. The 16 accounts that had nothing were backfilled with source 'backfill_unrecorded', null versions and false flags. Re-acceptance is storage only, as asked: legal_version_num(text) turns a dotted version into a sortable number and returns null for the old date style values, and get_admin_consent_gap(p_terms_version, p_privacy_version) buckets every account into current, below_current, not_accepted or no_record and lists up to 200 that are not current; the reprompt flow is still not built. Admin visibility: get_admin_overview gained accounts_without_consent beside accounts_total, shown on the Overview as a number that should be zero, and the Legal consent card in AccountDetail.tsx now leads with a status of Recorded or Never captured and says in words when consent was never captured instead of showing blanks. Measured after the migration on 2 August 2026: 24 rows for 24 accounts, 8 signup and 16 backfill_unrecorded; against Terms 1.0 and Privacy 1.0 that is 1 current (the test account), 8 below_current (the old 2026-03-14 rows) and 16 not_accepted.

CANCELLATION AT CHECKOUT. Before this release the Stripe Checkout session carried nothing about renewal or refunds, and the only self serve control was the hosted portal button labelled "Manage payment method", with no portal configuration recorded on the account. stripe-billing now sets custom_text.submit.message on the session so the customer reads, before paying, that the subscription renews automatically, that they can cancel at any time, that cancellation takes effect at the end of the period already paid for, and that fees are not refunded. Two new stripe-billing actions back a real in product cancel: "cancel" sets cancel_at_period_end on the active subscription, "resume" clears it. src/pages/Billing.tsx shows a Cancel subscription button next to the portal button, confirms once, and repeats the terms in plain words underneath.


## Legal documents (v3.32.1)
The hand written Terms.tsx and Privacy.tsx bodies are gone. They described the retired consulting product and said nothing about automated decision making in hiring, so the copy was deleted rather than edited.

The eight documents now hold their final text, not placeholders. Terms of Service 1.0 (21 sections), Privacy Policy 1.0 (14), Cookie Policy 1.0 (8), Security Overview 1.0 (11), Subprocessors 1.0 (5), Data Processing Agreement 1.0 (16), Service Level Agreement 1.0 (10) and Copyright Policy 1.1 (8). Copyright is at 1.1 because the delivered revision dropped the United States DMCA section 512 machinery for a plainer notice and counter notice process; the registry keeps "DMCA Policy" as an alias so an older cross reference still links. Terms, Privacy, the DPA and the SLA state an effective date of 1 August 2026; Cookies, Security, Subprocessors and Copyright state only a last updated date.

One renderer, eight documents. Each document is a markdown file at src/content/legal/<slug>.md, the filename being the slug, so a lawyer amends the text without touching a component. src/lib/legalDocs.ts is the registry (slug, path, navigation title, our own one line description for /legal, whether it sits in the footer, and the aliases a document is referred to by elsewhere), loads the markdown with import.meta.glob at build time, and holds parseDocMeta, stripDocHeader, buildToc, slugifyHeading and linkCrossReferences. parseDocMeta reads version, effective date and last updated date from either YAML style frontmatter or plain "Version 1.0", "Effective 1 August 2026" and "Last updated 1 August 2026" lines near the top, and invents nothing when a document states none of them; the page header shows the effective date when there is one and the updated date otherwise. stripDocHeader removes the title and those version and date lines from the rendered body, because the page header already carries all three and two version statements on one legal page is a liability. linkCrossReferences wraps a mention of another document in a link to it, changing no wording, and skips headings, fenced blocks and anything already inside a link.

src/components/legal/LegalPage.tsx renders it: react-markdown with remark-gfm (both already in the project, no new dependency) so tables work, a consistent header carrying version and date plus a Print button, a table of contents generated from the h2 and h3 headings whenever a document has ten or more h2 sections (Terms 21, DPA 16, Privacy 14, Security 11, SLA 10, and not the three shorter ones), an anchor id and a hover link icon on every heading with hash deep links scrolled into view, a 46rem measure, and the print rules in src/index.css under .legal-doc (tokens flattened to black on white, headings kept with their text, tables and list items not split across pages, 18mm by 16mm page margins).

Routes: /terms and /privacy are unchanged because they are indexed and linked from the signup checkbox, and are now two line wrappers around LegalPage. New: /cookies, /security, /subprocessors, /dpa, /sla, /copyright, plus /legal listing all eight with the version of each. The shared Footer links Terms, Privacy, Cookie Policy, Security and Subprocessors on every public page; the DPA, the SLA and the Copyright Policy exist and are reachable from /legal but stay out of the footer. All seven new routes are in public/sitemap.xml and robots.txt blocks none of them.

The consent record in src/lib/legal.ts (Terms 1.0, Privacy 1.0, effective 1 August 2026) matches what those two documents state, checked against the rendered pages.





## Auth emails (v3.43.0, rebranded and finally tracked)
`supabase/functions/auth-send-email/index.ts` is the Supabase Auth "Send Email" hook target — GoTrue calls it directly for every `signup`, `recovery`, `magiclink` and `email_change` event, verified via a `standardwebhooks` signature (`SEND_EMAIL_HOOK_SECRET`), and sends through Resend as `AYN <noreply@mail.aynn.io>`. Which GoTrue events route here is configured in the Supabase Dashboard's Auth Hooks settings, not in anything in this repo (`supabase/config.toml` has no `[auth.hook.*]` section). Before this fix the file had **no local source anywhere in this repository** — it was live on the server only, deployed straight to Supabase with no git history, the same "lives only on the server" gap the v3.27.0 migration-history fix closed for six admin functions. It is now committed here.

The template was rewritten from a pre-rebrand dark theme (`#0f0f0f` page, `#1a1a1a` card, white-on-dark CTA, plain "AYN. All rights reserved." footer) to match the `.lp` design tokens used everywhere else in the product (`src/index.css`): warm paper page (`#faf7f2`), white card with a `#ece5da` border, `#0b0b0c` ink, `#e85d3a` ember accent on the CTA button and as a small underline beneath the wordmark, and an `AYN AI` footer line matching `siteLinks.ts`'s `COPYRIGHT_LINE`. `admin-broadcast`'s `wrap()` (below) was the closest existing structural reference in the repo — same font stack, same card shape, same palette family — just missing an ember accent of its own until now. No functional code changed: webhook verification, the confirmation-URL builder (including the `/reset-password` redirect rewrite for `recovery`), and the Resend call are unchanged.

Two other auth-email-shaped things exist in this project and were confirmed to be dead ends, not touched: `custom-auth-emails` (also live-only, no local source) requires an admin JWT, which structurally can't be a target GoTrue calls itself, and has zero callers anywhere in `src/`; the `auth_email_templates` table it reads from (`supabase/migrations/20250829131247_*.sql` and two later edits) is seeded with old purple-gradient (`#667eea`/`#764ba2`) copy that nothing live ever actually shows a user. Also out of scope here: `send-email` (also live-only, called from `useEmail.ts` and `AuthModal.tsx`'s post-signup `welcome` email) is a separate transactional-email function with its own old white/black theme and a dead unused `password_reset` branch — it's a different code path from the actual sign up / sign in emails this fix targeted, flagged for a future pass rather than bundled in here.

No UI in this repo currently calls `signInWithOtp`/`verifyOtp` (`grep` for both returns zero hits in `src/`), so the `magiclink` branch of the template exists and is correctly rebranded but is unreachable from the current product; only password auth, password reset, and Google OAuth are live sign-in paths today.

Verified live: redeployed, read the deployed source back via the Supabase API and confirmed it matched the intended file byte for byte, then triggered a real signup through a throwaway account — the signup call came back a clean 200 with `confirmation_sent_at` set, which only happens if the (blocking) Send Email hook itself returned success, meaning the new template rendered and sent through Resend without error. Test account and its `profiles`/`terms_consent_log` rows deleted afterward.

## Support system
src/components/support/ (AISupportChat, FAQBrowser, TicketForm, TicketList, UserTicketDetail) with admin counterparts (SupportManagement, TicketDetailModal). `TicketForm` writes to `support_tickets`/`ticket_messages` and is embedded on both `/contact` (v3.53.0, open to signed-out visitors) and `/help`'s "Still stuck" section (links out to `/contact` rather than embedding its own copy). The old `contact_messages`-writing form this line used to describe is gone; it was removed in the v3.50.0 Contact rewrite.

## Legacy signing pages
/sign/:token (ClientSign) and /nda/:token (NDASign) are the last remnants of a retired document signing product. They still resolve, backed by the sign-document edge function, so links already sent do not break. No admin surface points at them any more.

## Admin app
/manage-bae76e99d97e188b mounts src/admin-app (AdminApp, adminSupabase client, useAdminQuery with adminRpc). /admin deliberately 404s. Six sections in src/components/admin/sections: Overview, Employers, Candidates, Marketplace, Money, System.

### Edge functions the admin depends on
admin-auth-pin: verifies a 4 to 6 digit PIN (`/^\d{4,6}$/`) with a server side lockout, mints the HMAC signed 8 hour admin ticket, re-verifies it through check { ticket }, and changes the PIN through set { pin, new_pin }. The login screen (src/admin-app/AdminApp.tsx PinScreen) only rendered 4 boxes and auto-submitted at 4 digits until v3.36.0; it now renders 6, auto-submits at 6, and submits a shorter entry on Enter or an Unlock button once 4 or more digits are in — the settings pane's own PIN-change fields (src/components/admin/sections/system/SystemPanes.tsx) already correctly accepted 4 to 6 and needed no change. admin-broadcast: sends an admin written email to a chosen audience, called from the System Email pane. Both were missing from the edge function list in CLAUDE.md until v3.27.0.

### Activity log, email delivery visibility, and a plan editor (v3.47.0)
Third round of "what am I missing as admin" fixes, after v3.46.0.

`get_admin_activity_log(p_limit)` (`supabase/migrations/20260803210000_admin_activity_log_and_plan_editing.sql`) is the first reader `security_audit_logs` has ever had. New System → Activity pane (`ActivityPane`, `src/components/admin/sections/system/SystemPanes.tsx`) shows a plain-English label per action (`ACTIVITY_LABELS`, falling back to the raw action name for anything unmapped), a one-line summary of the `details` jsonb (`summarizeActivityDetails`, pulling out `target_email`/`plan_key`/`reason`/etc. when present), and a high-severity badge.

`get_admin_plans()`/`admin_update_plan(...)` (same migration) let an admin change a plan's name, credits, proposals/assessments/searches limits, and visibility from a new "Edit plans" card in Money (`PlanEditor`, `src/components/admin/sections/system/ControlPanes.tsx`). `price_cents` and the Stripe price/product ids are read-only by design: Stripe prices are immutable once created, and grepping every edge function in this repo confirmed none of them has ever created or mutated a Stripe Price/Product object (only `customers`, `subscriptions`, `checkout.sessions`, `billingPortal.sessions`, `invoices` are touched) — editing the displayed price without a matching Stripe change would silently desync what the app shows from what Stripe actually charges. Verified live on `seeker_week`, the one seeker plan with zero real subscribers at test time: credits 30 → 31 → back to 30.

`get_admin_email_log(p_limit)` plus a new "Did these emails actually go out?" card in the Email pane (`EmailLogSection`) reads `email_logs`, the same table `admin-broadcast` has always written to but that the v3.43.0/v3.44.0 automatic emails never did. Wiring that up found a real bug: `auth-send-email`'s signup confirmation was failing to log itself on every single signup, a `23503` FK violation on `email_logs.user_id → auth.users(id)`, because the GoTrue Send Email hook fires before a brand-new signup's `auth.users` row is visible to the hook's own connection. Diagnosed by temporarily mirroring the real error into `security_audit_logs` (proven-working, readable via the new Activity pane) since this function's console output isn't otherwise visible. Fixed by dropping `user_id` from `auth-send-email`'s log writes; `recipient_email` alone identifies the recipient. `recovery`/`email_change` were never affected (always an existing user). `stripe-webhook`'s `sendReceiptEmail` and `resume-hub`'s `notifyCandidate`/`notifyOrgMembers` (see docs/map/resume-hub.md) both got the same `email_logs` write, without this bug, since they only ever email already-existing accounts. Verified live: a real signup now logs `sent` correctly; a real proposal notification also logged correctly and happened to genuinely fail in the same test run (Resend rejects the `@example.com` test domain), captured with Resend's real error message — proof the failure path works, not just the success path.

### Kill switch confirmation and admin management (v3.46.0)
Two gaps closed after the founder asked directly what the admin panel was missing.

`FlagsPane`'s switches used to call `set.mutate` (→ `admin_set_feature_flag`) the instant a switch moved, with no confirmation, even for "Whole platform" — one misclick puts every signed in user into maintenance immediately. Turning a switch OFF now runs `window.confirm` first, naming the switch and stating it takes effect right now; turning one back ON stays unguarded, since that direction restores service and gating it would only slow down recovering from a real incident.

New pane, System → Admins (`AdminsPane`, `src/components/admin/sections/system/ControlPanes.tsx`), backed by two new RPCs in `supabase/migrations/20260803200000_admin_role_management.sql`: `get_admin_admins()` lists every account with the `admin` role (email, when granted, last sign in, and `is_you` for the caller's own row), `admin_set_admin_role(p_user_id, p_grant, p_reason)` grants or revokes it. Before this, admin access could only be seen or changed by an engineer running SQL directly against `user_roles`. `admin_set_admin_role` refuses to target the calling admin's own account (`p_user_id = v_admin` check, same self-lockout guard `admin_suspend_account`/`admin_erase_account`/etc already use), requires a non-empty reason, and audits at `high` severity in `security_audit_logs` like every other admin moderation action. The UI reuses `useAdminAccounts`'s search (the same one Credits already uses) to find a person before granting, and `window.prompt` for the required reason on revoke, matching the low-ceremony-but-real-gate pattern the rest of System already uses (`window.confirm` on the broadcast send in Email, for example).

### Email pane self-documents (v3.45.0)
`src/components/admin/sections/system/SystemPanes.tsx`'s `EmailPane` gained `SystemEmailsReference`, a collapsible static card rendered above the existing broadcast composer (now labeled "Write your own message" so the two don't read as one tool). It lists all nine automatic emails from v3.43.0 (signup confirm, password reset, email change confirm) and v3.44.0 (payment receipt, proposal received, assessment received, proposal accepted, proposal declined, assessment completed): subject, who it goes to, when it fires, and a one-sentence plain-English description of what it says. Pure UI, no new backend call — the content is a hardcoded array (`SYSTEM_EMAILS`) since these are fixed templates, not data. Built because the admin has no engineering background and asked directly for product screens to explain themselves rather than requiring him to ask or read code.

### v3.41.0 fixes, full admin panel walkthrough
Asked directly to check the whole admin panel for bugs after v3.40.0's grant hardening. Walked all six sections and all 11 System sub-panes live against production. The grant changes themselves were confirmed unaffected everywhere; four unrelated real bugs turned up.

`admin_user_snapshot` (System → Credits pane's Snapshot panel) referenced `talent_pool_consent.tp.is_active`, a column that has never existed; every other place in the codebase uses `opted_in`. The function raised on every call, and `CreditsPane` only branched on `isLoading`, never `isError`, so the panel just showed permanent empty defaults with no visible error no matter which account was selected. Fixed the column reference.

`get_admin_rate_limit_stats` and `admin_unblock_user` (System → Rate limits pane, its load and its Unblock button) were both missing the `authenticated` GRANT every sibling admin function has, same shape of gap as the v3.27.0 `get_admin_terms_consent` bug. `get_admin_ai_cost_stats` was also found ungranted but is orphaned (zero callers; the real AI cost pane uses `get_admin_ai_usage`, already granted) and was left alone, matching the existing orphaned-function pattern (`delete-account`, v3.35.0).

`get_admin_overview`'s and `get_admin_accounts`'s "job seekers" counts both queried `SELECT count(*) FROM profiles WHERE coalesce(role,'job_seeker')='job_seeker'`, which only iterates rows that already exist in `profiles`. 31 of 58 real accounts have no `profiles` row (never completed onboarding) and were silently excluded, undercounting the Overview headline stat by more than half (27 shown vs 57 real). `get_admin_email_audience` already got this right with a LEFT JOIN from `auth.users`; matching that pattern alone was still wrong because one real employer account's `profiles.role` is stale at `'job_seeker'` (it signed up before the v3.36.0 role-on-signup fix, despite a real approved `employer_accounts` row) — `profiles.role` cannot be trusted as the seeker/employer split. Both functions now define "job seeker" as "absent from `employer_accounts`", independent of `profiles.role`.

Also the local dev origin gap that made admin panel testing impossible from this machine before any of the above could even be checked: `_shared/originGuard.ts` (the allowlist `admin-auth-pin` alone enforces; `resume-hub` stays wide open) listed `:5173`/`:8080` but this project's dev server runs on `:3000`, so every local PIN attempt CORS-failed and surfaced as a false "Incorrect PIN". Added `http://localhost:3000` to the allowlist.

Found and not a bug: `admin_adjust_credits`'s ledger `balance_after` can look wrong on an account's first-ever spend, because deducting from an account with no `subscriptions` row yet triggers a same-transaction `period_grant` (via `billing_ensure`) before the deduction lands. Real design quirk of `billing_ensure`/`credit_spend`, not something this pass touched; both functions do a plain read-then-write with no row locking (`20260731122450_*.sql`, `20260731235555_*.sql`), which is also a narrow but genuine real-overspend race under true concurrent spend calls, flagged for the founder rather than fixed here since it needs a locking strategy decision, not a one-line patch.

Verified live throughout with a real throwaway admin account: PIN entry through the real UI end to end, the Snapshot panel showing real data after the fix, the Rate limits pane loading, and Overview/Accounts both reading 57 seekers / 1 employer / 58 total after the fix, consistent with the Email pane's number all along.

### v3.36.0 fixes, live walkthrough
Six real bugs found by driving the production app end to end as a signed-out visitor, a real seeker, a real employer and admin, plus the credit bypass v3.35.0 had already flagged. Summarized in CLAUDE.md's v3.36.0 entry; the two schema-touching ones in full here.

erase_account_core (shared by self_delete_account and admin_erase_account) previously did `UPDATE llm_usage_logs SET user_id = NULL`, but that column is NOT NULL, so the whole erasure rolled back inside its own implicit transaction for any account with AI usage history — essentially every employer and every extension user. It now `DELETE`s those rows like every other content table in the function. Separately, it never touched `orgs` (no user_id column, only created_by), so erasing an employer left their company's orgs row behind indefinitely; it now captures the erased user's org memberships before deleting them, then deletes an org only when no member remains (`NOT EXISTS` against org_members), so a multi-seat org (schema supports it, unused in production today) survives erasing one member while a solo employer's org is correctly removed. Both verified live: a real llm_usage_logs row no longer crashes the function, a solo-member org is removed, a two-member org survives erasing one member with the other member's row intact.

handle_new_user_profile now reads `role` and `company_name` out of `raw_user_meta_data` (the same signup metadata AuthModal already attached and v3.33.0 already established the pattern for with consent) and, for an employer, also inserts the employer_accounts row server side inside the same trigger transaction as the account itself — previously both were attempted client side right after signUp() returned, which silently failed under RLS whenever email confirmation left no session yet (the normal production case), so every employer signup became an unrecorded job_seeker. AuthModal.tsx's post-signup block was deleted down to the consent IP call it still needs.

### Per account limit overrides and erasure (v3.29.0)
Two gaps left by v3.28.0, both per account, neither per plan nor global.

Limit overrides. account_limit_overrides holds proposals_limit, assessments_limit, searches_limit and monthly_credits for one user, each nullable, plus the reason and which admin set it. Null means fall back to the plan, which stays the default for everyone, and no plan value is ever copied into the row. Zero is a real zero and blocks the action. The one place that decision lives is effectiveLimit in resume-hub, which takes the override only when it is neither null nor undefined, so 0 survives; employerBilling loads the override next to the plan, planLimitReached uses the effective number, and the 402 plan_limit_reached message says the limit was set for this account rather than claiming the plan includes it. The credit path reads the same override: the stripe-webhook invoice.paid handler grants monthly_credits from the override when one is recorded and from the plan otherwise. An override is per user, so it survives a plan change and stays until an admin clears it, which is why the account detail view carries a Limit override badge and prints plan, override and what is in force for all four numbers. admin_set_limit_override and admin_clear_limit_override are admin only and both audit.

Erasure, two levels. admin_erase_account(p_user_id, p_reason, p_confirm_email) deletes the person's content across roughly fifty tables (resumes and resume_versions, cover letters, jobs and matches, user_profile_data and user_profile_canonical, candidate_index and candidate_skills, talent_pool_consent, extension tokens and link codes, learned answers and ask messages, chat, support threads, preferences and settings, restrictions and overrides, profiles and roles) plus their objects in the resumes, attachments, avatars and generated-files buckets, nulls the person out of the operational logs, and keeps what we are obliged to keep: credit_ledger and subscriptions for accounting, and the proposals and assessments an employer sent them, with the candidate reduced to an opaque candidate_ref of the form erased-xxxxxxxx and no name, email or phone. The auth user is banned for a hundred years, its email replaced with an erased+...@erased.invalid placeholder, phone and metadata cleared, identities and sessions deleted. admin_purge_account then removes the auth.users row itself, and only on an account that has already been erased. Both refuse unless has_role says admin, refuse to target yourself or another admin, require a reason, and require p_confirm_email to match exactly; the admin UI makes the admin type it with nothing pre filled and confirms twice before an erase. account_erasures records erased_at, purged_at, the reason and the email at the time, and the high severity security_audit_logs entries survive the purge because they hang off the admin, not the person.

Two things had to move for this to work. The dead on_security_event trigger on security_logs, a leftover of the agents deleted in v3.21.0, called a function that read a table that no longer exists, so every trigger driven security log write failed and blocked the profiles delete; it is dropped. And reveal_requests.candidate_user_id used to cascade from auth.users, so a purge silently deleted the employer's own proposal history; the column is nullable now with ON DELETE SET NULL, which leaves the row with only the opaque ref.

Verified live on 1 August 2026 with two throwaway accounts. An override of proposals 0 and searches 500 with the other two left empty read back as effective proposals 0, searches 500, assessments from the plan and monthly credits 6 from the plan, so zero and null are handled apart. A wrong confirmation email raised "The confirmation email does not match this account" on both erase and purge, erasing yourself raised "You cannot erase your own account", and an account temporarily given the admin role could not be erased and kept its resume. After erase: resumes, jobs, user_profile_canonical, candidate_index, talent_pool_consent and profiles were all at zero rows, credit_ledger and subscriptions still held their row, the proposal read candidate_ref erased-11111111, auth.users held the erased placeholder email with banned_until set, and the audit row existed. After purge: no auth.users row, the account_erasures row carried purged_at, the audit entries were still there, and on the second account the employer's proposal survived with candidate_user_id null and the opaque ref intact.

### Self service account tools (v3.34.0)

The Delete Account button in Settings had been dead since v3.21.0 deleted the edge function it posted to, functions/v1/delete-account, so it 404'd silently while the Privacy Policy kept promising deletion. The fix moved the work into the database rather than restoring an edge function: the removal routine admin_erase_account already used was pulled out into a shared erase_account_core(p_user_id, p_actor, p_reason), which does everything the v3.29.0 erasure did (roughly fifty tables, storage objects, operational logs nulled, credit_ledger and subscriptions kept for accounting, the employer's proposals and assessments kept with the candidate reduced to an opaque candidate_ref, the auth user banned and scrubbed). admin_erase_account was rewritten to call it, with its guards unchanged. REVOKE ALL keeps erase_account_core itself unreachable from anon or authenticated; only the two wrapper functions can call it.

self_delete_account(p_confirm_email) is the person's own path: it reads auth.uid(), refuses if nobody is signed in, refuses an admin account outright ("Admin accounts cannot be deleted from settings"), and requires the typed email to match the account's email case-insensitively before calling erase_account_core with themselves as both target and actor and the reason 'self service deletion'. It audits at high severity in security_audit_logs with the account's own id in the details, since the account itself is gone by the time anyone reads the log. GRANT EXECUTE is scoped to authenticated.

self_pause_account() is the lighter alternative offered in the same confirmation dialog, next to the destructive button: it sets talent_pool_consent.opted_in to false, nulls candidate_index.indexed_at so the person drops out of search immediately rather than waiting for the next reindex pass, and turns off all four email flags in user_settings (system alerts, usage warnings, marketing, weekly summary). Nothing is deleted.

self_export_account() returns one jsonb object: profile, career profile (user_profile_canonical), skills, resumes, tailored resume_versions, cover letters, saved jobs, job_matches, discovery consent, proposals (a fixed subset of reveal_requests columns), assessments (questions and answers, deliberately never the rubric or the score, matching the v3.13.0 rule that a candidate never sees their own score), settings, preferences, memory, the full legal consent log, subscription and the credit ledger.

Frontend: src/lib/account.ts wraps all three RPCs plus the DELETION_REMOVED and DELETION_KEPT copy arrays shown before anyone confirms, LOCAL_KEYS and clearLocalTraces (also where ayn_tutorial_completed, the leftover from the tutorial module deleted in v3.21.0, gets cleared), and downloadJson for the export file. src/components/settings/PrivacySettings.tsx replaces the dead POST: an Export button, the existing delete-chat-history control, and a delete dialog that lists what is removed and what is kept, offers "Pause instead" and "Download my data first" inline, and only enables the destructive button once the typed email matches exactly.

Every other functions/v1/ fetch in the frontend (resume-hub, stripe-billing, ayn-agent-society) was checked against the live edge function list; none of the rest are rotted the way this one was.

### Account moderation (v3.28.0)

The Accounts pane is no longer read only. Every row opens a detail view built from get_admin_account_detail: how the person signed up (provider from raw_app_meta_data, whether the email is confirmed and when, signup time), sign in history (last sign in, active session count), what they have done since (resume, profile completeness, discoverable, saved jobs, proposals received and sent, assessments received and taken, credits balance and credits spent) and account state (system role, account type, employer status, plan and subscription status). It is a picture only: nothing here signs in as anyone, and nothing reads assessment_rubrics or assessment_results, which stay service role only per v3.13.0.

Two levers sit beside that picture.

Suspension, all or nothing. admin_suspend_account(p_user_id, p_reason, p_until) sets auth.users.banned_until (a hundred years out when p_until is null) and writes an account_suspensions row; admin_restore_account clears both. A reason is mandatory, both write to security_audit_logs at high severity, and both refuse to touch your own account or another admin with a clear exception. Enforcement is server side: accountGate in resume-hub answers 403 with { code: "account_suspended", reason, until, message } before any action runs, on the web lane and the extension lane alike.

Restrictions, one capability at a time. account_restrictions holds four named capabilities per account with the reason and who set it: discovery (cannot appear in the talent pool, filtered inside employer_match and employer_skill_catalog, and talent_pool_set refuses a new opt in), proposals (an employer cannot send proposals), assessments (an employer cannot generate or send them) and ai (cannot spend credits on tailoring, cover letters, scoring or Ask AYN, which is the cost valve). admin_set_restriction(p_user_id, p_capability, p_on, p_reason) is the single switch. The same gate enforces them through ACTION_CAPABILITY, answering 403 with { code: "account_restricted", capability, reason, message }. Order of checks is global kill switch first, then suspension, then the capability.

The seeker sees the truth rather than a broken toggle: talent_pool_get returns discovery_restricted and the reason, and TalentPoolCard disables the switch and explains who removed them and why.

Verified live on 1 August 2026 against a real non admin account: suspend produced banned_until in 2126 and made resume-hub answer 403 account_suspended for talent_pool_get and hub_snapshot, restore cleared banned_until and both calls returned 200 again, a discovery restriction made talent_pool_get report discovery_restricted with the reason and made talent_pool_set refuse with 403 account_restricted, and suspending yourself or another admin raised "You cannot suspend your own account" and "You cannot suspend another admin".

### Settings, and the one maintenance mechanism (v3.27.0)
The Settings pane used to save ten system_config keys that nothing read: maintenance_mode, maintenance_message, maintenance_start_time, maintenance_end_time, pre_maintenance_notice, pre_maintenance_message, default_monthly_limit, require_approval, max_login_attempts, session_timeout. Flipping maintenance_mode did not put anything into maintenance. All ten controls are deleted and the rows removed from system_config, along with useAdminSystemConfig and useSetSystemConfig. Maintenance is the v3.25.0 kill switches and nothing else, and the pane now holds the admin PIN plus a link to that pane. Employer approval is unconditional by design, credit allowances come from the plans table, and sign in attempts and session length belong to Supabase auth, so none of them is a setting here.

### Migration history (v3.27.0)
Six admin functions existed only in the live database and are now recorded verbatim in supabase/migrations: admin_insert_ticket_message, admin_update_ticket, admin_upsert_system_config, get_admin_system_config, get_admin_error_monitoring, get_admin_support_tickets. Another 31 undocumented admin functions, every one a leftover of the admin deleted in v3.20.0 with no caller in src or supabase/functions, were dropped rather than recorded (custom orders, NDA, test results, visitor analytics, LLM management, message ratings, conversations, applications, beta feedback, credit gifts, churn alerts, dashboard stats, email broadcast users, user growth, subscriptions, system metrics and monitoring, AI limits, activity log, error logs and the *_data duplicates).

### Employer plan limits and statuses (v3.27.0)
plans.searches_limit is a real column and planLimitReached takes kind "search" next to "proposal" and "assessment": 25 searches per period on the free month, 100 Starter, 400 Growth, 1200 Scale. The old EMPLOYER_SEARCH_SOFT_CAP of 200 only applies to a plan that records no limit. employer_billing_get and the admin Employers list both report searches used against the allowance. employer_status gained 'declined': admin_employer_decline and the admin_employer_decide "decline" branch set it, 'suspended' is now reserved for an employer who was approved and then stopped, and EmployerPending words the two differently.


### The gate (v3.26.0)
Sign in, then user_roles must say 'admin', then the 4 digit PIN. Two things changed. The 'duty' role is gone from the gate: every admin RPC checks has_role(auth.uid(),'admin') only, so a duty user used to pass the PIN and then read "Admin access required" on every panel. Duty is now denied at the door. And the PIN can no longer be skipped from devtools: checkAdmin reads user_roles BEFORE it looks at any cached flag, and the cache no longer holds a user id it holds an HMAC signed ticket minted by admin-auth-pin on a correct PIN (payload userId.exp, signed server side, 8 hour window). A new admin-auth-pin action, check { ticket }, re-verifies it, so a hand written sessionStorage value fails and drops the browser back to the PIN screen.

Every admin read and write goes through adminRpc in useAdminQuery, which sets the Authorization header explicitly to dodge the "Multiple GoTrueClient" session conflict. The last hold out, the settings write in AdminPanel, now goes through useSetSystemConfig on the same path instead of calling supabase.rpc directly.

### Reindex (v3.26.0)
candidate_index.indexed_at and candidate_index.embedding_model are nullable. The admin Candidates reindex button calls admin_mark_candidates_stale, which nulls both columns; while they were NOT NULL that call would have thrown on the first real candidate. employer_match coerces with (r.embedding_model || FALLBACK_EMBED_MODEL), so a nulled row reads as stale against the current spec model and is picked up by the bounded 25 per run inline reindex.


### Kill switches (v3.25.0, real for the first time)
Six keys, every one defaulting to ON when absent: platform, candidate_search, proposals, assessments, tailoring, signups. The admin panel writes them with admin_set_feature_flag and the optional per switch message with admin_set_feature_message (max 300 chars), both into system_config under feature_flags and feature_flag_messages. Three readers:
1. Server. supabase/functions/resume-hub/index.ts calls get_feature_flags through the service client, caches the answer for 30 seconds, and gates work in featureGate. platform is checked on every gate, so a full stop covers everything, and the link_* auth actions run before the gate so people can still sign in and read the notice. ACTION_FLAG maps the rest: candidate_search covers employer_spec_extract, employer_skill_catalog and employer_match; proposals covers employer_draft_proposal and employer_reveal_request; assessments covers employer_assessment_generate, employer_assessment_send, assessment_start, assessment_answer and assessment_submit (reading history through assessment_list and employer_assessment_list is never gated); tailoring covers smart_tailor, ext_cover_letter_text and the web lane tailor and cover_letter. A blocked call answers 503 with { code: "feature_disabled", feature, message }.
2. Database. handle_new_user raises when signups is off, so account creation stops even outside the app. Existing people can still sign in.
4. Minimum extension version (v3.3.0). The same answer shape carries one more refusal: an extension build older than system_config.extension_min_version is answered 426 with { code: "extension_outdated", feature: "extension", min_version, your_version, message }. See docs/map/extension.md.

3. Frontend. get_feature_flags stays executable by anon so a signed out visitor sees a platform off message too. src/hooks/useFeatureFlags.ts polls every 60 seconds and shares one cache; src/components/shared/MaintenanceNotice.tsx renders the switch message in place of the feature (MaintenanceNotice, FeatureGate) or as a full screen (PlatformMaintenanceScreen, used by Index AuthedShell and ResumeHub). src/lib/featureError.ts turns a 503 feature_disabled body into FeatureDisabledError, refreshes the flag cache so the surface repaints as switched off, and the api wrappers in src/lib/employer.ts, resumeHub.ts and assessments.ts all throw it.

## Standalone
/resume-match uses the resume-match edge function (honest recruiter scoring, JSON verdict with comparisonRows). scripts/perpetual_predict.py plus .github/workflows/perpetual-predictions.yml: scheduled market prediction job writing snapshots (ayn_market_snapshot migration). public/sw.js service worker. e2e/ Playwright suites cover the platform (auth, dashboard, admin, security, stress); they target the platform, run them only when touching platform code.

## Servers
server.js: express static host for dist/ with SPA fallback and cache headers (/assets immutable, /frames 7 days). backend/server.py: FastAPI health stub only. Real backend = Supabase edge functions + RLS tables.

## Employer mode (v2.9.0-B)

Sign out (v3.39.0 fix, see docs/map/resume-hub.md "Sign out" for the full writeup): `EmployerHub.tsx`'s two Sign out buttons (desktop sidebar, mobile bottom nav) called a bare `supabase.auth.signOut()` with no await and no navigation, same bug and same fix as Resume Hub's — both now share one `handleSignOut` per file that awaits the sign-out then calls `navigate("/")`.

`src/components/dashboard/EmployerChatPanel.tsx` is a full-surface overlay opened from a "Hiring mode" button in the top right of the dashboard shell. It is entirely separate from the seeker chat pipeline: it does not go through `useAYN` / `ayn-ai-proxy`. All calls hit `src/lib/employer.ts`, which is a thin session-JWT wrapper around `supabase/functions/resume-hub` employer actions.

Flow inside the panel:
1. On open, `employer_org_get`. If no org, the assistant greets with the sales-oriented line "Search AYN's talent pool for candidates who match your open role. First, register your company so I can start finding candidates." above the inline register card; `employer_org_create` sets up the org + admin membership. (v2.9.1: same "Hiring mode" button in the dashboard shell, no separate route — clicking it just opens the panel directly on the register card for a user with no org.)
2. v3.8.0: no free-text intake. `employer_spec_extract` prefills a JobSpec from the optional opening description, then IntakeWizard asks the remaining questions as widgets (`employer_skill_catalog` backs the skill chips).
3. When JobSpec lands, an editable spec card exposes a "Find candidates" button → `employer_match`.
4. Results render as anonymized candidate cards with score ring, matched must-haves, gaps, and grounded "why" bullets. "Request intro" calls `employer_reveal_request`; contact reveal happens only after the candidate approves in ProfileTab.

Seeker-side surface (v2.9.1): `src/pages/ResumeHub.tsx` fetches `reveal_list` on mount and, if any request is pending, shows a small primary-colored count badge on the Profile rail icon (aria-labeled "N intro requests") and a "<n> companies want an intro" line at the top of the Talent Pool card in `ProfileTab.tsx`, so a seeker notices an incoming request without hunting for it.

