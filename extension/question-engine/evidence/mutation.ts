/** evidence/mutation.ts — the "mutation" evidence source for revealed nodes. Returns Evidence[]; never a Question. */
import type { Evidence } from "../evidence";
export function collect(el: Element, root: Document | Element): Evidence[] {
  void el;
  void root;
  return [];
}
